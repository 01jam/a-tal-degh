# In dev, contentUrl() builds root-relative paths (/api/content.yml,
# /contents/pixel/...) served straight off disk by the serveLocalContent vite
# plugin — no commit/push needed to see content or image changes. The
# GitHub raw host in .env is still used for the production build.
VITE_GITHUB_RAWCONTENT=
